namespace Asce.Managers.Utils
{
    public static class Constants
    {
        /// <summary>
        ///     Size of one pixel: 1/32
        /// </summary>
        public const float PIXEL_SIZE = 0.03125f;

        /// <summary>
        ///     When move input value is bigger than this then it is considered there is a movement input
        /// </summary>
        public const float MOVE_THRESHOLD = 0.05f;
    } 
}